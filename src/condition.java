public class condition {
    public static void main(String[] args) {
   int age =25;
   int income=500;
        System.out.println(age>=18 && income>=500 ? "your are eigible for the loan":"your are not eligible for the loan" );
    }
}
